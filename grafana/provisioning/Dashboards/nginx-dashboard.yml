apiVersion: 1

providers:
  - name: "nginx-dashboard"
    orgId: 1
    folder: "NGINX"
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards
